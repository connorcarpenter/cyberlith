[package]
name = "http_server"
version = "0.1.0"
authors = ["connorcarpenter <connorcarpenter@gmail.com>"]
workspace = "../../.."
edition = "2021"
publish = false

[features]

[dependencies]
http_common = { path = "../http_common" }
http_server_shared = { path = "../http_server_shared" }
http_client_shared = { path = "../http_client_shared" }
logging = { path = "../../logging" }
acme = { path = "../acme" }

async-dup = { version = "1.2.2" }