[package]
name = "random"
version = "0.1.0"
edition = "2021"
workspace = "../.."

[dependencies]
naia-socket-shared = { path = "../../../naia/socket/shared", features = [ "wbindgen" ] }
rand = { version = "0.8.5" }

log = { version = "0.4" }
